<div id="top-nav" class="<%= params[:controller] + params[:action] %>">
  <div class="container">
    <a href="<%= root_path %>">
      <img class="logo" src="<%= asset_path("brand/logo.svg") %>">
    </a>
    <ul>
      <% if user_signed_in?%>
        <li><%= link_to('Logout', destroy_user_session_path, :method => :delete) %>   </li>
      <% else %>
        <li><%= link_to('signin', new_user_session_path) %>   </li>
      <% end %>
    </ul>
  </div>
</div>
