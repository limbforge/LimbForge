<%= form_for(@amputation_level) do |f| %>
  <% if @amputation_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@amputation_level.errors.count, "error") %> prohibited this amputation_level from being saved:</h2>

      <ul>
      <% @amputation_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
